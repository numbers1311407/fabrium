$("td.resource-pending[data-user=<%= resource.id %>]").each(function () {
  $(this).closest("tr").find("[href$=activate]").<%= resource.pending? ? "show" : "hide" %>();
  $(this).html(function () {
    return $(this).data("<%= resource.pending? ? "yes" : "no" %>");
  });
});
